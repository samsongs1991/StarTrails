export default class MarkerManager {
    constructor(map) {
        this.map = map;
        this.markers = {};
    }

    updateMarkers(trails) {
        console.log("UPDATE MARKERS IS WORKING");
    }
}